<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>SMS_Client_Secret__c</fullName>
    <label>SMS Client Secret</label>
    <type>Text</type>
    <length>255</length>
    <description>Zoom Marketplace app Client Secret used by ZoomSMSIntegration - a separate app from ClientId__c/ClientSecret__c (same Zoom account, different app/credentials), so it has its own field rather than overloading the main one.</description>
</CustomField>
