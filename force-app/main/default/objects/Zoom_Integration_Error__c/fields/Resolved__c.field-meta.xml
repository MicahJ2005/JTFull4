<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Resolved__c</fullName>
    <label>Resolved</label>
    <type>Checkbox</type>
    <defaultValue>false</defaultValue>
    <description>False only while a retry is still pending (Recording_Id__c populated, Next_Retry_At__c not yet reached). True for every other logged error, and set true once a retry succeeds or is given up on.</description>
</CustomField>
