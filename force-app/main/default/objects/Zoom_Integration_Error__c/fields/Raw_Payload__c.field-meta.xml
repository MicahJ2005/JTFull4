<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Raw_Payload__c</fullName>
    <label>Raw Payload</label>
    <type>LongTextArea</type>
    <length>32768</length>
    <visibleLines>5</visibleLines>
    <description>JSON-serialized webhook payload captured when a retryable failure needs to rebuild the original request later (e.g. VoiceCall creation) - unlike Recording_Id__c-based retries, this failure mode has no simpler identifier to re-fetch the data from.</description>
</CustomField>
